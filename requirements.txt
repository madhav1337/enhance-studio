# Enhance Studio dependencies (tested on Python 3.14, Windows/macOS/Linux).
# The versions in comments are what this was verified against; the >= floors
# keep it installable on a range of Python versions.
Flask>=3.0                     # tested 3.1.3
opencv-python-headless>=4.9    # tested 4.13.0.92  (headless build: no desktop GUI libs, server-friendly)
numpy>=1.26                    # tested 2.4.6
Pillow>=10.0                   # tested 12.2.0
